#!/usr/bin/env i5

proc main() {
    let result = divide(2, "string") ? {
        $print err
        $exit(1)
    }
    $print result
}

func divide(a b) ! {
    if b == 0 {
        throw "can not divide by zero!"
    }

    return a / b ? {
        throw the error
    }
}
