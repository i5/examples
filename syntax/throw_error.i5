import {
    "salifm/std" as std
}

proc main() {
    let result = divide(2, 0) ? {
        std.print("error: " : error.msg)
        std.os.exit(1)
    }

    std.print(result)
}

func divide(a b) ! {
    if b == 0 {
        throw std.error("can not divide by zero!")
    }

    return a / b
}
