#!/usr/bin/env i5

proc main() {
    let result = divide(2, "string") ? {
        $print "error: " : error.(msg)
        $exit(1)
    }

    $print result
}

func divide(a b) ! {
    if b == 0 {
        throw $error("can not divide by zero!")
    }

    return a / b
}
