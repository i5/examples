$use {
	stdl.fmt
}

enum {
	Red
	Green
	Blue
}

proc main() {
	print-color(Green)
}

proc print-color(color) {
	when color {
		is Red {
			fmt.println("red")
		}
		is Green {
			fmt.println("green")
		}
		is Blue {
			fmt.println("blue")
		}
	}
}
