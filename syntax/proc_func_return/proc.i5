proc print1() {
    std->println(1)
}

proc print_something(something) {
    std->println(something)
}

proc run_procedure(procedure) {
    procedure()
}

proc run_procedure_with_args(procedure, arg1, arg2) {
    procedure(arg1, arg2)
}
