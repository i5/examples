import std
import process

proc main() {
    con result = divide(2, 0) ? {
        std.println("error: " & err.msg)
        process.exit(1)
    }
    std.println(result)
}

func divide(a, b) ! {
    if b == 0 {
        throw std.error("can not divide by zero!")
    }
    return a / b
}
