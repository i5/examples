import std

proc main() {
    var output = ""
    for i in 1..100 {
        mut output &= (
            match 0 {
                i % 15 => "FizzBuzz"
                i % 3  => "Fizz"
                i % 5  => "Buzz"
            } else => i
        ) & "\n"
    }
    std.println(output)
}
